class Model:
    def __init__(self, name):
        self.name = name
        self.weights = tensor([0.1, 0.2, 0.3])

    def train(self, X):
        print(self.name + " is training with data: " + str(X))
        print("Weights:", self.weights)

m = Model("MyAI")
m.train([1, 2, 3])