from models import LogisticRegression

def bias(data):
    return data + 1

model = LogisticRegression()
data = @[[1, 2], [3, 4], [5, 6], [7, 8]]
labels = @[0, 0, 1, 1]
model.train(data, labels)

result = data |> bias |> model.predict
print(result)